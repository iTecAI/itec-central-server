{
    "logLevel": "DEBUG",
    "logFormat": "{levelname} - {processName}:{pathname}:{funcName}:{lineno} @ {asctime} > {message}",
    "logFile": null,
    "projectRoot": "projects",
    "pipPath": "pip",
    "workerPoolLimit": 32,
    "mongoDB": {
        "host": "localhost",
        "port": 27017,
        "database": "ICSTEST"
    }
}